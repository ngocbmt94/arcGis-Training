export enum eDataDMA {
  storeKey = "dataDMA",
  sectionKey = "dataDMASection",
}
export enum ePage {
  storeKey = "pagination",
  sectionKey = "paginationSection",
}
export enum eSelectedItem {
  storeKey = "selected",
  sectionKey = "selectedSection",
}
